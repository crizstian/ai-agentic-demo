apiVersion: v1
kind: Service
metadata:
  name: mcp-financial-data
  namespace: harnessbank-demo-end2end
  labels:
    app: mcp-financial-data
    tier: internal
spec:
  type: ClusterIP
  selector:
    app: mcp-financial-data
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
